export interface UserModel {
    id: string;
    firstName: string;
    lastName: string;
    pic: string;
}

export interface UserFormModel {
    id: string;
    firstName: string;
    lastName: string;
    password: string;
    oldPassword: string;
    pic: string;
}
